# IP Connectivity

Topics to cover:
- Static routing, default routes
- OSPF basics (single-area), routing concepts (next hop, routing table)
- Route selection and administrative distance

Add routing lab configs and examples here.